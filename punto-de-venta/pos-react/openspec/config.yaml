# OpenSpec config — pos-react (TPV-365 reconstruction workspace)
# Auto-generated by sdd-init (2026-08-13)

project:
  name: TPV-365 POS Rebuild
  domain: tpv-365.com.mx
  description: >
    Reconstruction of the POS (punto de venta) frontend whose source was lost.
    Reference base: pos-react-seguridad/ (React 18 + Vite 5).
    Target folder: tpv365-rebuild/ (to be created).
  backend:
    provider: supabase
    project_ref: kdyflbexrensqzbquxyu
    schema_backup: ~/supabase-backup-20260812.sql

stack:
  framework: react
  react_version: "18.2"
  build_tool: vite
  vite_version: "5.1"
  language: javascript
  module: esm
  key_deps:
    - "@supabase/supabase-js"
    - zustand
    - styled-components
    - react-router-dom v6
    - "@tanstack/react-query"
    - "@tanstack/react-table"
    - react-hook-form
    - sweetalert2
    - react-icons

testing:
  strict_tdd: false
  test_command: null
  reason: No test runner found in reference base (no vitest/jest/@testing-library)

sdd:
  mode: strict_tdd
  phase_rules:
    - proposal -> specs -> design -> tasks -> apply -> verify -> archive
    - strict_tdd requires tests-first when runner exists; none detected, standard mode

conventions:
  atomic_structure: src/components/{atomos,moleculas,organismos,templates}
  barrel: single central src/index.js re-exports all modules
  state: zustand stores in src/store/*Store.jsx (create((set, get)))
  backend: supabase RPC pattern in src/supabase/crud*.jsx (supabase.rpc("fn", params))
  storage_bucket: imagenes (paths "entidad/{id}")
  alerts: sweetalert2 (Swal.fire) for errors/success
  auth: supabase.auth OAuth google; ProtectedRoute accessBy authenticated/non-authenticated
  env_vars: VITE_APP_SUPABASE_URL, VITE_APP_SUPABASE_ANON_KEY
  styling: styled-components + src/styles/{variables,themes,breakpoints,GlobalStyles}
  file_ext: .jsx
