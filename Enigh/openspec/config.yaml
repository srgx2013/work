# OpenSpec SDD Configuration — ENIGH IKTAN Simulator
# Auto-generated by sdd-init

project:
  name: Enigh
  display_name: ENIGH IKTAN Simulator
  description: >
    React-based simulator for the IKTAN electronic data capture system used by
    INEGI (Mexico's National Institute of Statistics and Geography) for the
    ENIGH (National Survey of Household Income and Expenditures). The app
    allows interviewers to practice data capture with realistic mock data.
  domain: survey-data-capture

stack:
  type: frontend-only
  framework: React
  bundler: Vite
  language: TypeScript
  test_runner: vitest
  strict_tdd: true
  node_version: ">=18"

sdd:
  phase: init
  artifact_store: engram
  mode: interactive

conventions:
  - Conventional commits
  - Clean Architecture (screaming)
  - Container-Presentational pattern
  - Spanish domain model (UI labels, survey questions)
  - English code identifiers (functions, variables, types)

testing:
  runner: vitest
  library: "@testing-library/react"
  strict_tdd: true
  coverage:
    statements: 80
    branches: 75
    functions: 80
    lines: 80

project_phases:
  1: Study & theoretical exam preparation (complete)
  2: IKTAN data capture simulator (current — this SDD)
  3: Not yet defined
